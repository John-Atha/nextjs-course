import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
};

const CustomersPage = () => {
  return <p>Hey, I am the customers dashboard</p>;
};

export default CustomersPage;
